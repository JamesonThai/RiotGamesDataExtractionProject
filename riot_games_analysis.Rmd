---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 

Add a new chunk by clicking the *Insert Chunk* button on the toolbar or by pressing *Ctrl+Alt+I*.

When you save the notebook, an HTML file containing the code and output will be saved alongside it (click the *Preview* button or press *Ctrl+Shift+K* to preview the HTML file).

The preview shows you a rendered HTML copy of the contents of the editor. Consequently, unlike *Knit*, *Preview* does not run any R code chunks. Instead, the output of the chunk when it was last run in the editor is displayed.

Install the following packages if they're not already installed

```{r}
  package.list <- c("ggplot2", "tidyverse")
  new.packages <- package.list[!(package.list %in% installed.packages()[,"Package"])]
  if (length(new.packages)) {
    install.packages(new.packages, dependencies = TRUE)
  } else {
    print("packages checked")
    library(ggplot2)
    library(tidyverse)
    library(tictoc)
  }
```

Getting dataframes from specific files
```{r}
  tic("match.cores")
  if (!exists('match.cores')) match.cores <- read_csv("data/dataframes/matchcores.csv")
  toc()
  tic("match.parti")
  if (!exists('match.parti')) match.parti <- read_csv("data/dataframes/matchparticipants.csv")
  toc()
  tic("match.stats")
  if (!exists('match.stats')) match.stats <- read_csv("data/dataframes/matchstats.csv")
  toc()
  tic("time.events")
  if (!exists('time.events')) time.events <- read_csv("data/dataframes/timelineEvents.csv")
  toc()
  tic("time.partis")
  if (!exists('time.partis')) time.partis <- read_csv("data/dataframes/timelineParticipants.csv")
  toc()
```

# Brief Intoduction into the dataset

## match.cores
  Match cores has the following columns
  1. gameCreation
  2. gameDuration
  3. gameVersion
  4. mapId
  5. gameMode
  6. gameType
  7. seasonId
  8. platformId
  9. gameId
  

```{r}
  head(match.cores)
```

We will be looking at class gameMode and where the mapId is 11 because we're mainly looking at classic game mode on Summoner Rift. For now will be running python code again because I forgot to include what game type it is being a ranked or normal game.


```{r}
  head(match.parti)
```

```{r}
  head(match.stats)
```

```{r}
  head(time.events)
```

```{r}
  head(time.partis)
```

